/**
 * Compatibility re-export. Real Cocos entry lives in ui/cocos/GameBootstrap.
 * Kept so older scene notes still resolve; prefer ui/cocos/GameBootstrap.
 */
export { GameBootstrap } from './cocos/GameBootstrap';
